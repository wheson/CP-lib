snippet extgcd
abbr 拡張ユークリッド
    template <typename T>
    T ExtendGcd(T a, T b, T &x, T &y) {
        x = 1, y = 0;
        int g = a;
        if(b != 0) {
            g = ExtGcd(b, a % b, y, x);
            y -= a / b * x;
        }
        return g;
    }
